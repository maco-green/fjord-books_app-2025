<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('books.show.edit_link'), edit_book_path(@book) %> |
  <%= link_to t('books.links.back_to_index'), books_path %>
  <%= button_to t('books.show.destroy_link'), @book, method: :delete %>
</nav>
