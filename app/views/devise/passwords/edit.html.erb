<h2><%= t("views.auth.change_password_title") %></h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <%= f.hidden_field :reset_password_token %>

  <div class="field">
    <%= f.label :password, t("views.auth.new_password") %><br />
    <% if @minimum_password_length %>
      <em><%= t("views.auth.password_minimum", count: @minimum_password_length) %></em><br />
    <% end %>
    <%= f.password_field :password, autofocus: true, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation, t("views.auth.confirm_password") %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="actions">
    <%= f.submit t("views.auth.change_password") %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
