<div id="<%= dom_id report %>">

  <p>
    <strong><%= t('reports.report.title') %></strong>
    <%= report.title %>
  </p>

  <p>
    <strong><%= t('reports.report.body') %></strong>
    <%= report.body %>
  </p>

</div>
